name: Deploy to InfinityFree

on:
  push:
    branches:
      - main  # Branche à partir de laquelle déployer

jobs:
  ftp-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Récupère le code du dépôt
      - name: Checkout code
        uses: actions/checkout@v3

      # 2️⃣ Debug - liste tous les fichiers pour vérifier que GitHub voit ton site
      - name: Debug - list files in site
        run: ls -R site

      # 3️⃣ Upload via FTP
      - name: Upload website via FTP
        uses: SamKirkland/FTP-Deploy-Action@4.3.0
        with:
          server: ftpupload.net
          port: 21
          username: if0_40433041
          password: Almax1202
          server-dir: /htdocs/
          local-dir: site/
